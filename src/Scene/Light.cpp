#include "Light.hpp"

Light::Light(const glm::vec3 &position, const glm::vec3 &color, const float &radius)
        : _position(position),
          _color(color),
          _radius(radius) {
    //
}
